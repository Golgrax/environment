[project]
name = "environment-py"
version = "0.1.0"
description = "Python abstraction for environment management and metrics"
authors = [{ name = "Bekian" }]
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
