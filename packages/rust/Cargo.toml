[package]
name = "octovel-environment"
version = "0.1.0"
description = "A lightweight and cross-platform Rust library to safely interact with the system, user, and process environment through a unified, type-safe API."
license = "Apache-2.0"
repository = "https://github.com/octovel/environment/"
readme = "README.md"
keywords = ["env", "environment"]
categories = ["development-tools"]
edition = "2024"

[dependencies]
